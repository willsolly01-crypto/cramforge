{
  "permissions": {
    "allow": [
      "Bash(git add *)",
      "Bash(git commit -m 'Fix: sync materials immediately after upload *)",
      "Bash(git push *)",
      "Bash(git commit -m 'Fix: preserve local materials when merging server state *)"
    ]
  }
}
